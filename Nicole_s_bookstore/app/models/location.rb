class Location < ApplicationRecord

  #def shelf_limit_checker
    #Location.where(storage_type: "shelf").each do |shelf|
    # ExactPlacement.find_by(location_id: :shelf_id) each do |exact_shelf|
    #   if exact_shelf.books.size > 6,
    #     put the book in the back
    #end
    #end
    #end
  #end
end
