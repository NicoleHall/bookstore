<div class="container">

  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= form_for(@book) do |f| %>

        <%= f.label :where_would_you_like_to_move_this_book %>
        <%= f.text_field :location_id, value: '', class: 'form-control' %>

        please type "table" "shelf" or "back"
        <%= f.submit "Save changes", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>

  <%= link_to "Back to Inventory", root_path %>
</div>
